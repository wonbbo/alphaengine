# =============================================================================
# AlphaEngine v2 보안 설정
# =============================================================================
# 이 파일을 config/secrets.yaml로 복사하고 실제 값을 입력하세요.
# secrets.yaml은 반드시 .gitignore에 추가하세요!

# -----------------------------------------------------------------------------
# 모드 설정
# -----------------------------------------------------------------------------
# production: 실거래
# testnet: Binance Testnet
mode: testnet

# -----------------------------------------------------------------------------
# Binance API - Production
# -----------------------------------------------------------------------------
production:
  api_key: "your_production_api_key"
  api_secret: "your_production_api_secret"

# -----------------------------------------------------------------------------
# Binance API - Testnet
# -----------------------------------------------------------------------------
testnet:
  api_key: "your_testnet_api_key"
  api_secret: "your_testnet_api_secret"

# -----------------------------------------------------------------------------
# Web 보안
# -----------------------------------------------------------------------------
web:
  secret_key: "your_jwt_secret_key_here"

# -----------------------------------------------------------------------------
# Slack 알림 (선택)
# -----------------------------------------------------------------------------
# Slack Incoming Webhook URL
# 생성: https://api.slack.com/messaging/webhooks
slack:
  webhook_url: ""  # 비워두면 Slack 알림 비활성화
  channel: ""      # 채널 오버라이드 (선택, 빈 문자열이면 Webhook 기본값 사용)

# -----------------------------------------------------------------------------
# Upbit API (입출금용)
# -----------------------------------------------------------------------------
# Upbit에서 발급받은 API Key/Secret
# 권한: 전체 (자산조회, 주문, 출금)
upbit:
  api_key: ""
  api_secret: ""
  trx_deposit_address: ""  # Upbit TRX 입금 주소 (출금 시 사용)

# -----------------------------------------------------------------------------
# Binance TRX 주소 (입출금용)
# -----------------------------------------------------------------------------
# Binance에서 발급받은 TRX 입금 주소 (TRC20 네트워크)
binance:
  trx_deposit_address: ""  # Binance TRX 입금 주소 (입금 시 사용)

# -----------------------------------------------------------------------------
# 전략 설정
# -----------------------------------------------------------------------------
# 봇 시작 시 자동 로드할 전략
# module: 전략 모듈 경로
# class: 전략 클래스 이름
# params: 전략 파라미터 (default_params 오버라이드)
# auto_start: 자동 시작 여부 (기본 true)
#
# 주의: 매매 수량(quantity)은 secrets.yaml에서 설정하지 않음
# - 전략 내부에서 리스크 기반으로 동적 계산 (예: 2% 룰, ATR 기반)
# - 자세한 내용은 docs/strategy/README.md 참조
strategy:
  module: "strategies.examples.sma_cross"
  class: "SmaCrossStrategy"
  auto_start: true
  params:
    fast_period: 5
    slow_period: 20
    use_market_order: true
